export interface Feedbacks {
  changePassword: string;
  recoverPassword: string;
  logoutDevices: string;
  user: {
    created: string;
    updated: string;
  };
  person: {
    created: string;
    updated: string;
  };
  noRegisters: string;
}
