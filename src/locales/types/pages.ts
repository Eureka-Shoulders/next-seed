export interface Pages {
  users: {
    list: string;
    create: string;
    edit: string;
  };
  people: {
    list: string;
    create: string;
    edit: string;
  };
  noPermissions: {
    title: string;
    description: string;
    buttonLabel: string;
  };
  notFound: {
    title: string;
    description: string;
  };
}
